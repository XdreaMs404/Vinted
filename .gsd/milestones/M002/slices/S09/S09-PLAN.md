# S09: High-Throughput Proxy Pool + Webshare Acquisition Operator Flow

**Goal:** Turn a large Webshare proxy pool into a real multi-route acquisition path with safe operator ergonomics.
**Demo:** After this: After this: operators can load a local Webshare proxy pool without rewriting entries by hand, preflight exit-route diversity plus Vinted API reachability, and run discovery/runtime on a real multi-route transport instead of one-hot failure rotation.

## Tasks
