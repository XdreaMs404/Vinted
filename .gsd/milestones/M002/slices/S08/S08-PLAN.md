# S08: Native API Price Bounds for Discovery

**Goal:** Reduce wasted catalog traffic and ban pressure by pushing price windows directly into the discovery API.
**Demo:** After this: After this: `batch` and `continuous` can constrain both min and max price directly at the Vinted API boundary, while persisted requested URLs and client-side price guards still show the real acquisition contract.

## Tasks
