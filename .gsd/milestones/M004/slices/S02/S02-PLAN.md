# S02: Stable Grouped Reads via PostgreSQL + ClickHouse Parity

**Goal:** Persist grouped truth into stable read models that survive refreshes and serving cutovers.
**Demo:** After this: After this: grouped leaders, comparisons, and detail reads stay stable across refreshes and backend read paths because they are projection-backed, not recomputed ad hoc.

## Tasks
