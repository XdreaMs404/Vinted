# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | scope | Market coverage focus | Vinted Homme and Femme categories plus all reachable sub-categories only | Keeps the first radar focused enough to cover deeply and compare contextually. | Yes — if a later milestone explicitly expands marketplace scope | agent |
| D002 | M001 | arch | Data acquisition backbone | Public collection only, no login required, and no central dependency on undocumented private APIs | Matches access constraints and reduces fragility risk. | Yes — if an official supported data channel becomes available and complements public collection | agent |
| D003 | M001 | pattern | Truth surface in product | Separate observed facts, inferred conclusions, and uncertain states everywhere that matters | Product credibility depends on making evidence and uncertainty explicit. | No | agent |
| D004 | M001 | pattern | Primary product view | Mixed dashboard with market summary first and listing-level proof immediately underneath | The product must tell a market story and let the user verify it quickly. | No | agent |
| D005 | M001 | operability | Local runtime modes | Support both batch mode and continuous mode in M001 | Batch supports testing and controlled reruns; continuous mode is required for a living radar. | No | agent |
| D006 | M001 | data | Listing state model posture | Use a prudent state machine covering active, sold observed/probable, unavailable non-conclusive, deleted when distinct, and unknown | Avoids overclaiming from ambiguous public signals. | Yes — if live evidence later justifies tighter or broader state distinctions | agent |
| D007 | M001 | data | Ranking split | Provide separate “demande pure” and “premium” rankings | These answer different market questions and should not collapse into one score. | No | agent |
| D008 | M001 | pattern | Premium score weighting | Price acts as an intelligent contextual factor, not the dominant driver | The premium ranking should reward strong performance at relatively high price, not simply expensive listings. | No | agent |
| D009 | M001 | arch | Milestone sequencing | Build listing-level credibility first, then richer market reading, then product-level intelligence plus AI, then SaaS hardening | Preserves the evidence backbone before richer analytics and commercialization. | Yes — if execution proves a different sequence is materially safer or faster | agent |
| D010 | M001/S01 planning | arch | S01 collector delivery stack | Implement S01 as a Python package with a Typer batch CLI, public vinted.com HTML extraction over HTTP, SQLite persistence, and targeted raw evidence fragments rather than full-page archives by default. | The repository is a blank slate, public SSR HTML already exposes enough catalog and item data for S01, SQLite keeps the system local-first, and targeted evidence retention preserves explainability without creating unnecessary archive volume. | Yes — if public HTML extraction stops being viable or later slices need broader evidence retention. | agent |
| D011 | M001/S01 completion | architecture | Public acquisition shape for S01 | Use Vinted's server-rendered `/catalog` HTML plus the embedded `catalogTree` flight payload for seed sync, and ingest listing cards from SSR `new-item-box__container` fragments into SQLite with targeted raw evidence retention. | Live verification showed the public catalog page already exposes the full Homme/Femme category tree and enough listing-card fields to bootstrap discovery without browser automation or undocumented private APIs. This keeps the collector local-first, lowers anti-bot surface area, and preserves inspectable evidence without full-page archives. | Yes | agent |
| D012 | M001/S02 completion | data-model | Observation history granularity for S02 | Persist `listing_observations` as one normalized observation per listing per run, while keeping `listing_discoveries` as the per-sighting diagnostic layer. | Per-run observations make first seen/last seen/cadence/freshness truthful and cheap to query, while the detailed discovery rows still preserve the lower-level evidence needed for debugging category/page behavior. | Yes | agent |
| D013 | M001/S03 completion | state-machine | State evidence precedence for S03 | Treat direct public item-page probe evidence as highest-priority state input: 404/410 map to `deleted`, parsed buy-state flags map to observed `active`/`sold`/`unavailable`, and only unresolved cases fall back to cautious history-based inference from follow-up misses. | This keeps state claims grounded in the strongest public signal available while preserving prudent fallback behavior when public item pages are inconclusive or unchanged. It also keeps browserless collection viable by reusing escaped script data already present in the public HTML. | Yes | agent |
| D014 | M001/S04 completion | scoring | S04 ranking model posture | Keep `demand` and `premium` as separate on-demand score surfaces; demand is anchored in state/history evidence, and premium reuses demand as the base score with only a modest contextual price boost when the peer group clears explicit support thresholds. | This preserves the distinction between sell-through and high-price performance, keeps price from dominating the premium ranking, and avoids fake contextual precision when peer support is thin. | Yes | agent |
| D015 | M001/S05 completion | ui-architecture | S05 dashboard delivery architecture | Serve the local dashboard as a server-rendered standard-library WSGI app with matching JSON diagnostics, reusing repository/state/scoring payloads instead of duplicating business logic in client code. | This keeps the product local-first and dependency-light, and makes the UI more truthful because the browser surface and the debug endpoints are driven by the exact same server-side payload assembly. | Yes | agent |
| D016 | M001/S06 completion | runtime-architecture | S06 local operator runtime observability architecture | Persist each batch or continuous radar cycle in SQLite `runtime_cycles` and expose that same runtime truth through `runtime-status`, `/api/runtime`, and the dashboard runtime card. | Continuous mode needs durable phase/error visibility without diverging into separate scheduler state files; keeping runtime-cycle truth in SQLite lets the CLI and dashboard read the same source of truth and preserves failure context when a cycle dies mid-run. | Yes | agent |
| D017 | 2026-03-22 review follow-up | architecture | Primary acquisition backbone after the March 2026 review | Use Vinted's private `/api/v2/catalog/items` endpoint as the primary discovery surface, while keeping public item pages as the separate state-evidence path. | The user explicitly chose to assume the private API tradeoff. It materially improves catalog discovery throughput and field richness, and the project already retains public item-page probes as an independent evidence layer for cautious state resolution. | Yes | agent |
| D018 | 2026-03-22 DB corruption follow-up | operability | How synchronized SQLite databases are promoted into the local working path | Create a consistent remote snapshot first, copy it to a temporary local file, run database health checks, then atomically rename it into place. | The project runs SQLite in WAL mode and handles very large live databases. Directly copying into `data/vinted-radar.db` can expose partial, WAL-incomplete, or corrupted transfers as if they were usable local truth. | Yes | agent |
| D019 | M002 roadmap planning | architecture | M002 delivery stack | Keep the existing Python + SQLite + server-rendered WSGI architecture for M002 and deepen repository-driven SQL payloads instead of doing a framework rewrite. | The codebase already has the right brownfield seams in RadarRepository, the WSGI route split, and SQLite runtime persistence. The milestone risk is product shape, payload scalability, runtime truth, and VPS serving discipline—not a missing frontend framework. | Yes | agent |
| D020 | M002/S01 planning | architecture | How the M002/S01 overview home retires the current home-path scale risk | Implement S01 around a repository-owned SQL overview contract for `/`, while keeping the existing WSGI route topology and `/api/dashboard` seam as the brownfield compatibility surface. | The current default home still loads listing-state inputs across the full corpus and assembles market summary modules in Python. S01 needs to retire that primary-path risk without a framework rewrite or a second persisted state cache. Reusing the existing `/`, `/explorer`, and `/api/...` route split while moving overview summaries and first comparison modules into repository SQL keeps the change incremental, testable, and compatible with later S02-S05 slices. | Yes | agent |
| D021 | M002/S01/T01 | architecture | How the M002/S01 overview payload derives state counts and first comparison modules without full-corpus scoring. | Implement the overview home contract as a repository-owned `overview_snapshot()` built from SQL state classification that reuses `state_machine` thresholds/constants, and keep thin-support comparison rows visible with explicit low-support flags instead of filtering them away. | This retires the current home-path scale risk while keeping the evidence vocabulary aligned with the existing Python state machine. Surfacing low-support rows and module reasons preserves honesty and makes empty/thin modules diagnosable in JSON instead of disappearing silently. | Yes | agent |
| D022 | M002/S01/T02 | architecture | Brownfield compatibility posture for the M002/S01 overview home | Keep `/api/dashboard` as the machine-readable compatibility endpoint while making `/` a SQL-backed French overview, and source featured listing drill-downs from the SQL explorer page instead of the legacy scored home payload. | This preserves existing dashboard diagnostics and caller integrations while retiring full-corpus Python recomputation from the primary home path. It also keeps overview drill-down links SQL-first end to end, so future polish on `/` does not quietly reintroduce the old scoring-heavy assembly path. | Yes | agent |
| D023 | M002/S02 planning | operability | How S02 exposes pause/resume control versus runtime visibility | Make pause/resume a CLI-driven, DB-persisted control surface in S02 and keep `/runtime` plus `/api/runtime` read-only visibility surfaces. | The dashboard is currently GET-only and the slice's primary risk is truthful persistence and orchestration, not HTTP mutation handling. CLI controls are lower churn, easier to test, and still let the runtime page, overview, and API read the same persisted truth. | Yes | agent |
| D024 | M002/S02 planning | operability | How S02 exposes pause/resume control versus runtime visibility | Make pause/resume a CLI-driven, DB-persisted control surface in S02 and keep `/runtime` plus `/api/runtime` read-only visibility surfaces. | The dashboard is currently GET-only and the slice's primary risk is truthful persistence and orchestration, not HTTP mutation handling. CLI controls are lower churn, easier to test, and still let the runtime page, overview, and API read the same persisted truth. | Yes | agent |
| D025 | M002/S02 planning | architecture | How M002/S02 represents current runtime truth | Add a separate persisted `runtime_controller_state` snapshot/table for current controller and scheduler truth, while keeping `runtime_cycles` as immutable per-cycle history. | Scheduled, paused, next-resume, elapsed-pause, and heartbeat truth cannot be represented honestly by overloading the last cycle row. A separate controller snapshot fits the brownfield SQLite schema/migration model, lets the dashboard and scraper agree across separate processes, and preserves cycle history semantics. | Yes | agent |
| D026 | M002/S02 | runtime-architecture | How continuous mode orders post-cycle controller transitions versus observer callbacks | Persist the next controller state (`scheduled` / `paused`) before observer-facing callbacks after each continuous cycle. | Live S02 smoke proved that calling observer callbacks before writing the next controller state can create a transient truth gap between `/api/runtime`, `/health`, and the overview/runtime UI. Persisting the transition first keeps all read surfaces aligned on the same SQLite truth. | Yes | agent |
| D027 | M002/S03 | operability | How S03 represents mounted VPS serving and route verification | Use a shared `base_path` + optional `public_base_url` serving contract across the CLI, SSR route generation, health payloads, systemd installer, and the `verify_vps_serving.py` smoke verifier. | The live product now needs to work behind a reverse-proxy prefix without localhost-era links or route drift. One shared serving contract keeps HTML, JSON diagnostics, CLI output, and operator docs aligned, while the smoke verifier gives future agents one repeatable proof command for the mounted path. | Yes | agent |
| D028 | M002/S04 | architecture | How M002/S04 makes `/explorer` the main browse-and-compare workspace without reintroducing request-time full-corpus reshaping. | Build S04 around one repository-owned classified explorer snapshot that feeds filters, paging, comparison modules, and detail-navigation context, while keeping thin-support rows visible with explicit honesty metadata instead of suppressing them. | A single SQL-first explorer contract keeps filtering, paging, support counts, and drill-down links truthful across `/explorer`, `/api/explorer`, overview modules, and listing detail. Preserving the active explorer query in detail links closes the analytical loop, and leaving low-support rows visible avoids fake precision or silent disappearance of weak slices. | Yes | agent |
| D029 | M002/S06 completion | acquisition-honesty | How M002/S06 represents degraded acquisition truth across product and runtime surfaces | Persist state-refresh degradation summaries on each runtime cycle and aggregate them with latest discovery scan failures in `RadarRepository.runtime_status().acquisition`; keep JSON diagnostics literal and translate only the visible product wording in `dashboard.py`. | S06 needs one durable honesty contract for anti-bot hits, degraded probes, and failed scans. Keeping that truth in repository/runtime payloads prevents overview, explorer, detail, runtime, `/api/runtime`, and `/health` from drifting into page-local heuristics, while the product layer can still translate the same signals into broader-audience French copy. | Yes | agent |
| D030 | M002/S05 architecture restored during M002/S06 closeout | detail-product-architecture | Restore the S05 detail-route architecture decision in the append-only register after the S06 decision was appended | Keep `/api/listings/<id>` as the raw proof contract but add a narrative/provenance layer in `dashboard.py`, and render the HTML detail route as narrative-first with progressive proof disclosures. | The S05 architectural choice is still active project knowledge and should remain explicitly recorded alongside the new S06 degraded-acquisition decision. Keeping both decisions visible prevents future agents from inferring that S06 replaced the narrative/progressive-proof detail architecture. | Yes | agent |
| D031 | M002/S08 | acquisition | How discovery should apply price filters to Vinted catalog scans | Thread configured min/max price bounds into Vinted's catalog API request and retain a local post-download price guard before persistence. | Applying price bounds at the API boundary reduces wasted HTTP traffic and ban pressure, while the local guard preserves correctness if Vinted still returns a few out-of-range cards. | Yes | agent |
| D032 | M002/S09 | architecture | How M002/S09 should turn a large Webshare proxy pool into real acquisition throughput without leaking credentials | Adopt a gitignored local proxy-file contract (`data/proxies.txt`), normalize raw `host:port:user:pass` entries centrally, drive discovery through route-local sync/async sessions with per-route warm-up/throttling/cooldowns, and expose only safe pool metadata plus a `proxy-preflight` operator check | The existing retry-only proxy rotation barely used a large pool and forced operators to rewrite Webshare exports by hand. Route-local transport state lets concurrent discovery actually spread across the pool, while the local file contract keeps credentials out of the repo and the runtime/CLI surfaces stay honest by persisting only pool size and transport mode. A preflight command closes the operator loop by proving exit-route diversity and Vinted reachability before longer runs. | Yes | collaborative |
| D033 | M002 roadmap extension after S09 / VPS storage-growth incident | architecture | Long-term storage and analytics architecture after the SQLite growth failure | Adopt a polyglot data platform: PostgreSQL for mutable control-plane and current-state truth, ClickHouse for analytical/serving facts and rollups, and S3-compatible object storage with Parquet for immutable raw evidence; keep SQLite only as a migration/backfill source, not the live durable boundary. | The live VPS incident showed that one successful continuous cycle can grow the cleaned SQLite database by roughly 1.5 GB while the current schema duplicates heavyweight card payload JSON across listings, listing_discoveries, and listing_observations. SQLite is no longer a credible long-term boundary for mutable runtime state, append-heavy analytical history, and raw evidence retention at the same time. Splitting those concerns gives bounded OLTP state in PostgreSQL, low-latency analytical serving and rollups in ClickHouse, and cheap durable cold retention in Parquet on object storage. This also creates AI-ready feature surfaces without forcing future models to scan raw event history directly. | Yes — vendor and table-format details can evolve, but the separation between mutable state, serving analytics, and immutable raw evidence should remain. | collaborative |
| D034 | M002/S10/T01 | architecture | How the polyglot platform connection/config contract is represented in code | Use one dataclass-based `vinted_radar.platform.config` loader for PostgreSQL, ClickHouse, and S3-compatible settings, including normalized object prefixes, explicit schema versions, redacted observability output, and cutover flags that all default to false. | Future bootstrap, migration, doctor, and outbox slices need one validated source of truth for platform endpoints and versions, but the current product must not silently switch live reads or writes. A single loader keeps the contract consistent and testable while preserving a safe staged-cutover posture. | Yes | agent |
| D035 | M002 S10 T02 | architecture | How bootstrap and doctor track schema/object-store readiness for the polyglot platform foundation | Use versioned `V###__name.sql` directories with per-provider `platform_schema_migrations` ledgers, plus persistent `.prefix` marker objects and write/delete probes for the configured S3-compatible prefixes. | This keeps bootstrap idempotent across PostgreSQL, ClickHouse, and object storage, makes expected schema versions observable from config, and exposes drift or missing write access before later outbox/manifest slices depend on the platform. | Yes | agent |
| D036 | M002/S10/T03 | architecture | How the polyglot platform identifies events/manifests and guarantees idempotent multi-sink delivery | Use canonical-JSON versioned event envelopes with deterministic UUIDv5 IDs, deterministic SHA-256 manifest checksums/IDs, and a PostgreSQL outbox keyed by unique `(event_id, sink)` rows with leased claim/retry state. | Future ingestion and projection slices need one write-once contract that can be replayed safely into multiple sinks without per-sink dedupe heuristics. Canonical payload hashing keeps the envelope and manifest bodies stable, while the outbox lease/retry state makes delivery observable and idempotent across retries and parallel workers. | Yes | agent |
| D037 | M002/S10/T04 | verification-architecture | How S10 proves and renders platform health end to end | Use env-substituted host ports, unique Docker compose project names, and shared health-rendering helpers so the real smoke stack stays isolated and platform-bootstrap/platform-doctor expose the exact readiness text the smoke proof asserts. | The live Docker-backed proof surfaced real workstation-specific collisions and setup drift risks. Isolating host ports and compose project names keeps the smoke path repeatable on shared machines, while shared health rendering prevents the CLI and tests from drifting into different platform-readiness vocabularies. | Yes | agent |
| D038 | M002/S11/T01 | architecture | Listing-card raw evidence contract for catalog ingestion | Persist versioned minimal evidence envelopes (`schema_version`, `evidence_source`, `fragments`) for both HTML and API cards, and keep normalization backward-compatible with legacy flat payloads. | This removes heavyweight API payload copies from the hot mutable path while preserving the targeted fragments needed to explain normalized listing fields. Backward-compatible normalization prevents historical observation rows and older fixtures from becoming unreadable when the evidence contract evolves. | Yes | agent |
| D039 | M002/S11/T02 | architecture | How evidence-lake batches are published and referenced across object storage and manifests | Anchor each evidence batch on a deterministic batch EventEnvelope, upload that JSON plus the partitioned Parquet object under deterministic keys, and reject same-key rewrites when the recorded SHA-256 differs. | This keeps the Parquet lake immutable and idempotent, lets EvidenceManifest stay compatible with the existing event_id/outbox seam, and gives downstream slices one stable object graph to inspect or replay: batch event JSON -> Parquet batch -> manifest. | Yes | agent |
| D040 | M002 S11 T03 | architecture | How collector services enable evidence-lake emission and surface emitted batches | Discovery and state-refresh now auto-create a shared collector evidence publisher from platform cutover flags in the default factories, emit deterministic Parquet/manifests batches when object-storage writes are enabled, publish outbox rows when Postgres writes are enabled, and expose emitted batch metadata on service reports. | This keeps the default runtime path connected to the new evidence lake without forcing platform dependencies in tests or local direct-mode runs, preserves idempotence through deterministic event identities plus immutable object keys/outbox conflicts, and gives downstream operators and slices a concrete report surface for manifest/event inspection. | Yes | agent |
| D041 | M002/S11/T04 | architecture | Historical evidence export event typing for the Parquet lake | Emit legacy SQLite discovery, observation, and probe exports under explicit `vinted.backfill.*` batch event types with `capture_source=sqlite_backfill` metadata instead of reusing live collector event names | This keeps historical lake exports distinguishable from live discovery/state-refresh evidence during replay, debugging, and future S14 cutover/backfill work, while still reusing the deterministic event and manifest contracts introduced in S10. | Yes | agent |
| D042 | M002/S11/T04 | observability | Evidence lookup inspection posture for manifested Parquet batches | Decode canonical-JSON Parquet cell strings back into navigable nested `row.*`, `event.*`, and `manifest.*` fragments during evidence inspection | The immutable lake stores many evidence/details cells as canonical JSON strings inside Parquet rows. Decoding them on lookup keeps the lake inspectable down to concrete fragments like `row.raw_card.price.amount` or `row.detail.reason` without bespoke per-dataset parsers. | Yes | agent |
| D043 | M002/S12/T01 | architecture | How PostgreSQL mutable truth is decomposed for control-plane and current-state projection | Use separate PostgreSQL tables for mutable manifests, discovery runs, runtime cycles/controller state, catalogs, listing identity, listing current state, listing presence summaries, and composite-key outbox checkpoints, with natural primary keys and projector-facing provenance fields (`last_event_id`, `last_manifest_id`). | The SQLite codebase already distinguishes stable listing identity, current state, recent presence, runtime controller truth, and cycle history. Preserving that separation in PostgreSQL keeps projector upserts idempotent on natural keys, avoids re-duplicating raw evidence blobs in hot mutable tables, and gives later runtime/CLI cutover work explicit checkpoint and provenance surfaces for replay, lag inspection, and troubleshooting. | Yes | agent |
| D044 | Cross-device GSD 2 setup for the Vinted project | tooling | How the llm-credential-failover extension is distributed across machines for this project | Version the extension as a project-local GSD extension under .gsd/extensions/llm-credential-failover and keep credentials/backoff state local in ~/.gsd/agent/ | The extension was previously installed only as a global per-machine GSD extension, so cloning the GitHub repository on another machine did not reproduce the setup. Moving the code into the repository makes the behavior portable with git pull while avoiding committing auth.json, OAuth tokens, or other per-machine secret state. | Yes | collaborative |
