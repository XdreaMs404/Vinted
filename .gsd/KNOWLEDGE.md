# Project Knowledge

Append-only register of project-specific rules, patterns, and lessons learned.
Agents read this before every unit. Add entries when you discover something worth remembering.

## Rules

| # | Scope | Rule | Why | Added |
|---|-------|------|-----|-------|
| 1 | acquisition | Keep raw evidence at the card-fragment level by default (`overlay_title`, visible subtitle/price texts, canonical URL, image alt) rather than archiving full pages. | It preserves explainability for listing normalization without ballooning local storage or diff noise. | 2026-03-17 |
| 2 | product-ui | Future dashboard work must default to a French, broad-audience product surface that separates market overview, full listing exploration, listing detail, and runtime administration instead of one compact technical page. | The user explicitly rejected the current dense dashboard and wants a usable market intelligence product, not a payload viewer. | 2026-03-18 |
| 3 | operations | Never promote a synchronized SQLite file straight into `data/vinted-radar.db`; create a remote snapshot, copy to a temporary local path, run DB health checks, then rename atomically. | The app uses WAL and large live files; direct copies can expose partial or unhealthy transfers as if they were usable local truth. | 2026-03-22 |
| 4 | platform-migrations | Keep provider-specific platform schema-version defaults instead of one shared default constant when PostgreSQL and ClickHouse migrations can advance independently. | S10/T03 added a PostgreSQL V002 outbox migration while ClickHouse stayed on V001; a shared default would have made local bootstrap/doctor expect a non-existent ClickHouse migration. | 2026-03-28 |
| 5 | platform-dev-setup | On this Windows workstation, pin `psycopg[binary]` instead of bare `psycopg` for local PostgreSQL-backed platform bootstrap/smoke runs. | The real T04 stack proved that plain `psycopg` could import but could not load a usable `libpq` wrapper here, causing `platform-bootstrap` to fail before migrations ran. | 2026-03-28 |
| 6 | auto-mode-git | Auto-mode has standing user approval to commit and push completed slice work to the configured Git remote as part of slice closeout. | The user explicitly approved repository pushes during auto-mode so execution should not stall on repeated confirmation prompts at each slice boundary. | 2026-04-09 |
| 7 | auto-mode-vps | Use `bash scripts/vpsctl.sh ...` for project VPS access; it targets `root@46.225.113.129`, loads the passphrase from the gitignored `.env.vps`, and should only re-prompt if authentication fails or the host changes. | Auto-mode needs one durable, inspectable VPS access path for benchmarking, deployment, and verification without re-asking for the same approved host on every task. | 2026-04-09 |
| 8 | acquisition-benchmark-cli | Acquisition benchmark spec files must resolve relative `db_path` values from the spec file directory, not from the caller's current shell location. | This keeps benchmark bundles portable when later slices generate specs/artifacts in nested directories or on copied VPS result bundles. | 2026-04-09 |
| 9 | windows-verification | On this Windows workstation, run repo verification with `python -m ...` in bash-driven flows and keep a repo-local `python3.cmd` shim for cmd-style gates that insist on `python3`. | `python` is available here, but `python3` resolves to a broken Windows app alias unless the repo provides its own shim, so verification can fail on launcher resolution rather than code health. | 2026-04-09 |
| 10 | vps-benchmark | On the live VPS, the default host `python3 -m vinted_radar.cli ...` path currently aborts with `ModuleNotFoundError: typer`; use `--remote-python /root/Vinted/venv/bin/python` (or install the missing dependency into system Python) before expecting long benchmark windows to run. | The real T04 baseline capture reached the VPS but collapsed to a single failed cycle because the runner invoked the host Python instead of the project virtualenv, producing a zero-throughput baseline artifact in about two seconds. | 2026-04-09 |
| 11 | acquisition-benchmark-bundles | Re-render saved `scripts/run_vps_benchmark.py` outputs by pointing `acquisition-benchmark-report --input` at the full bundle JSON; the CLI now reads the nested top-level `benchmark_report` directly. | Future M003 slices will compare and inspect saved VPS bundles repeatedly; requiring manual JSON extraction would be needless glue and an easy place for operators or agents to drift from the canonical artifact shape. | 2026-04-09 |

## Patterns

| # | Pattern | Where | Notes |
|---|---------|-------|-------|
| 1 | The public `/catalog` HTML embeds the full Homme/Femme catalog tree inside `self.__next_f.push(...)`, and listing cards are already server-rendered under `div.new-item-box__container`. | `vinted_radar/parsers/catalog_tree.py`, `vinted_radar/parsers/catalog_page.py` | Seed discovery and listing ingestion can stay browserless until Vinted removes or materially reshapes these SSR surfaces. |
| 2 | Keep per-run listing history in `listing_observations` and retain `listing_discoveries` as the lower-level per-sighting debug layer. | `vinted_radar/repository.py`, `vinted_radar/db.py` | This split keeps cadence/freshness queries cheap without losing page/category-level evidence when debugging collection behavior. |
| 3 | Public item pages expose `can_buy`, `is_closed`, `is_hidden`, and `is_reserved` inside escaped script text; normalizing escaped quotes before matching keeps item probes browserless. | `vinted_radar/parsers/item_page.py`, `vinted_radar/services/state_refresh.py` | Distinct 404s plus these buy-state flags are the current direct evidence surface for S03 state resolution. |
| 4 | Keep premium scoring demand-led and only apply a contextual price boost when the selected peer group clears explicit support thresholds. | `vinted_radar/scoring.py` | This avoids fake precision from thin peer groups while still surfacing contextually expensive fast movers. |
| 5 | Drive the dashboard HTML and JSON diagnostics from the same server-side payload assembly so the browser view stays debuggable and truthful. | `vinted_radar/dashboard.py`, `vinted_radar/cli.py` | S05 relies on server-rendered HTML plus `/api/dashboard` and `/api/listings/<id>` exposing the exact payload behind the page. |
| 6 | Persist operator runtime state in `runtime_cycles` and let both the CLI and dashboard read that table instead of inventing separate scheduler state files. | `vinted_radar/repository.py`, `vinted_radar/services/runtime.py`, `vinted_radar/dashboard.py` | S06 keeps batch/continuous phase, counts, and last-error truth in SQLite, surfaced through `runtime-status` and `/api/runtime`. |
| 7 | Treat catalog-first acquisition as the preferred scaling posture and use listing detail pages only as a targeted fallback, not as the default per-item path. | acquisition strategy / future M002+ planning | The target volume makes detail-page-per-item collection too expensive even before anti-bot friction; future work should maximize confirmed catalog fields and derived signals first. |
| 8 | Treat the primary image URL timestamp as `published_at_estimated`, never as an exact publication timestamp. | future listing model / dashboard | Fresh items can align within minutes, but long-tail drift is too large to present as exact truth. |
| 9 | Treat Cloudflare / Turnstile challenge rate as a first-class acquisition signal rather than assuming that a successful live run means the current HTTP path scales cleanly. | acquisition / runtime planning | Live access from this environment fell into challenge pages under raw HTTP, Chrome, Selenium, and Playwright even while earlier runs had partially succeeded. |
| 10 | Use `https://www.vinted.fr/api/v2/catalog/items` as the primary catalog discovery surface, and keep public item pages as a separate targeted evidence path for state resolution. | `vinted_radar/services/discovery.py`, `vinted_radar/services/state_refresh.py` | The project now explicitly accepts the private API tradeoff for discovery throughput while preserving page-level state evidence as an independently inspectable layer. |
| 11 | Keep the market-summary dashboard and the broad listing explorer as separate surfaces; the explorer should page directly from SQL instead of loading the full scored corpus just to browse listings. | `vinted_radar/dashboard.py`, `vinted_radar/repository.py` | This keeps the summary view evidence-rich while giving M002 a scalable listing-browsing path over large corpora. |
| 12 | Keep SQL overview state classification numerically aligned with `state_machine.py` by importing shared thresholds/confidence constants into `repository.py`, and treat `tests/test_overview_repository.py` as the drift alarm when either side changes. | `vinted_radar/state_machine.py`, `vinted_radar/repository.py`, `tests/test_overview_repository.py` | M002/S01 now classifies overview rows in SQL for scalability, so changing Python-only state thresholds without updating the repository constants/tests will silently desync the home payload from the state CLI/detail surfaces. |
| 13 | Keep the overview home SQL-first by building the summary from `overview_snapshot()` and sourcing featured listing cards from `listing_explorer_page()` rather than the legacy scored-home helpers. | `vinted_radar/dashboard.py`, `vinted_radar/repository.py` | This preserves the main-path scalability win from M002/S01 even when `/` offers drill-down links into `/explorer` and `/api/listings/<id>`. Future home-page work should extend those repository seams instead of reintroducing full-corpus score loading. |
| 14 | Keep current scheduler truth in `runtime_controller_state` and keep `runtime_cycles` as immutable history; dashboard, CLI, and health surfaces should read `runtime_status()` instead of inferring current state from the latest cycle row. | `vinted_radar/repository.py`, `vinted_radar/services/runtime.py`, `vinted_radar/dashboard.py`, `vinted_radar/cli.py` | M002/S02 needs truthful `running` / `scheduled` / `paused` / `failed` operator state. Overloading the latest cycle row reintroduces drift as soon as the loop is healthy-but-waiting. |
| 15 | Drive shared-shell links and mounted route examples through `RouteContext` / route diagnostics instead of hardcoding `/`, `/explorer`, `/runtime`, or `/listings/<id>` inside page-specific HTML. | `vinted_radar/serving.py`, `vinted_radar/dashboard.py`, `scripts/verify_vps_serving.py` | M002/S03 proved that mounted `/radar` serving stays trustworthy only when the CLI, HTML shell, `/health`, and smoke verifier all read from the same route contract. Future UI routes should extend that contract instead of inventing local hrefs. |
| 16 | Keep overview drill-downs, explorer comparison links, and listing-detail back navigation on the same `ExplorerFilters` / query-string contract rather than inventing route-local parameters. | `vinted_radar/dashboard.py`, `tests/test_dashboard.py` | M002/S04 only stays truthful if the active analytical lens survives overview → explorer → listing detail round-trips. Future filter or route work should extend the shared helpers/tests instead of hand-building hrefs in each surface. |
| 17 | Keep listing-detail proof semantics raw in JSON and translate only the visible HTML proof copy in `dashboard.py`, so diagnostics stay literal while the product surface stays readable. | `vinted_radar/dashboard.py`, `/api/listings/<id>`, `tests/test_dashboard.py` | M002/S05 improved the product detail route without mutating `state_machine.py` or `scoring.py`. Future detail/runtime copy work should keep this split instead of pushing UI-language concerns into the core evidence engines. |
| 18 | Keep degraded acquisition truth repository-owned in `runtime_status().acquisition` and thread its summary into overview/explorer/detail/runtime surfaces from there, while translating only the visible product wording in `dashboard.py`. | `vinted_radar/repository.py`, `vinted_radar/dashboard.py`, `/api/runtime`, `/health` | M002/S06 only stays honest if anti-bot hits, scan failures, and inconclusive probes come from one persisted/runtime-backed contract. Future degraded-mode work should extend that contract instead of inventing page-local heuristics. |
| 19 | Push `price_from` / `price_to` into the Vinted catalog API whenever a price window is configured, but keep the client-side price guard before `upsert_listing` as a local safety net. | `vinted_radar/services/discovery.py`, `vinted_radar/services/runtime.py`, `vinted_radar/cli.py` | M002/S08 reduces wasted catalog traffic and ban pressure without trusting the upstream API to respect bounds perfectly. |
| 20 | Treat large proxy pools as route-local transport state with per-route warm-up, throttling, and cooldown, and keep the operator contract on a gitignored local pool file (`data/proxies.txt`) or explicit `--proxy-file` input rather than hand-rewriting provider exports at invocation time. | `vinted_radar/http.py`, `vinted_radar/proxies.py`, `vinted_radar/cli.py` | M002/S09 only reached real throughput once discovery stopped pinning the whole async run to one active proxy. Future acquisition work should extend the route-local transport model instead of reintroducing one-hot proxy rotation. |
| 21 | Keep ClickHouse migration SQL database-agnostic and bind the migration runner to the configured target database instead of hardcoding `vinted_radar` in migration files. | `vinted_radar/platform/bootstrap.py`, `infra/clickhouse/migrations/` | This keeps bootstrap reusable across local/VPS environments and prevents config drift when the ClickHouse database name changes. |
| 22 | Parameterize the Docker data-platform stack with env-substituted host ports and unique compose project names, then assert health through the shared CLI renderers instead of bespoke smoke-test strings. | `infra/docker-compose.data-platform.yml`, `tests/conftest.py`, `vinted_radar/platform/health.py` | This avoids workstation port collisions during real PostgreSQL/ClickHouse/MinIO smoke runs and keeps operator-facing diagnostics and test expectations on one authoritative readiness surface. |
| 23 | Evolve listing-card proof via a versioned `schema_version` + `evidence_source` + `fragments` envelope, but keep `normalize_card_snapshot()` compatible with legacy flat HTML/API payloads. | `vinted_radar/card_payload.py`, `vinted_radar/repository.py` | Listing observations persist longer than parser contracts; tightening the hot-path evidence shape must not orphan historical rows or old test fixtures. |
| 24 | Treat object-store lake keys as immutable: stamp every uploaded object with SHA-256 metadata and skip/reject rewrites by comparing `head_object` checksums before uploading again. | `vinted_radar/platform/object_store.py`, `vinted_radar/platform/lake_writer.py` | The Parquet lake and manifest graph stay idempotent only if duplicate publishes can reuse identical objects safely and refuse same-key different-byte corruption. |
| 25 | When collector services auto-wire evidence-lake publishing from platform cutover flags, close the service itself rather than only closing `service.repository`. | `vinted_radar/services/discovery.py`, `vinted_radar/services/state_refresh.py`, `vinted_radar/services/runtime.py`, `vinted_radar/cli.py` | The default factories can now own Postgres/S3 clients through `CollectorEvidencePublisher`; skipping the service-level close path can leak platform connections even when the SQLite repository is closed. |
| 26 | Backfill legacy SQLite evidence under explicit `vinted.backfill.*` batch event types with `capture_source=sqlite_backfill` metadata instead of reusing live collector event names. | `vinted_radar/services/evidence_export.py`, `vinted_radar/domain/events.py` | This keeps historical lake exports distinguishable from live discovery/state-refresh batches during replay, debugging, and future cutover/backfill work. |
| 27 | Decode JSON-like Parquet cell strings back into nested structures before walking `row.*`, `event.*`, or `manifest.*` field paths in evidence lookup surfaces. | `vinted_radar/services/evidence_lookup.py`, `vinted_radar/cli.py` | Parquet batches store canonical JSON cells as strings; decoding them on inspection keeps immutable lake storage drillable down to fragments like `row.raw_card.price.amount` and `row.detail.reason` without bespoke one-off parsers. |
| 28 | Never derive `follow_up_miss_count` from `vinted.discovery.listing-seen.batch` page manifests alone; only catalog-complete projection inputs can safely mark listings as missed. | `vinted_radar/services/projectors.py`, `vinted_radar/platform/postgres_repository.py` | Page batches only prove which listings were seen on one page. Treating absent listings from the same catalog as misses at page scope would overcount misses across multi-page scans and corrupt sold-probable inference. |
| 29 | When runtime/control-plane truth is redirected to an external PostgreSQL repository, prove the cutover by asserting SQLite `runtime_cycles` and `runtime_controller_state` remain empty during the smoke run. | `tests/test_runtime_service.py`, `vinted_radar/services/runtime.py` | A green runtime cycle alone is not enough; this assertion catches silent fallback to legacy SQLite mutation paths while PostgreSQL mutable truth is supposed to own the control plane. |
| 30 | Preserve incomplete auto-mode recovery handoffs under `*-ASSESSMENT.md` (or another non-canonical artifact name), never under `*-SUMMARY.md`. | `.gsd/milestones/*/slices/*/tasks/`, GSD auto-mode state reconciliation | `state.js` reconciles `*-SUMMARY.md` as completed work. Leaving a timeout/recovery handoff under the canonical summary filename can silently flip pending tasks or slices back to done. |
| 31 | Treat `platform_mutable_manifests` as manifest-global rather than sink-scoped; multi-sink replay safety should use sink-specific checkpoints or row-level idempotency keys instead of overwriting one manifest status row. | `vinted_radar/platform/postgres_schema/__init__.py`, `vinted_radar/platform/clickhouse_ingest.py`, `vinted_radar/services/projectors.py` | The table key is only `manifest_id`, so a second projector sink would clobber the first sink’s status if it tried to persist independent projection state there. |
| 32 | When a query backend returns nested `latest_probe` evidence instead of flat `latest_probe_*` columns, derive the explorer/overview presentation fields in `dashboard.py` from either shape rather than assuming one storage layout. | `vinted_radar/dashboard.py`, `vinted_radar/query/overview_clickhouse.py`, `tests/test_clickhouse_parity.py` | The ClickHouse adapter preserved probe truth under `latest_probe`, while the SQLite explorer page exposed flattened probe columns. Normalizing at the dashboard serialization boundary keeps cutover parity without forcing every backend to mimic the repository’s internal row shape. |
| 33 | Full historical cutover should backfill PostgreSQL mutable truth directly from SQLite, publish live-compatible discovery/probe batches only for ClickHouse replay, and keep legacy observation/runtime exports as Parquet audit evidence rather than forcing them through serving-fact schemas they were never designed for. | `vinted_radar/services/full_backfill.py`, `vinted_radar/services/postgres_backfill.py`, `vinted_radar/platform/clickhouse_ingest.py`, `vinted_radar/services/evidence_export.py` | The current ClickHouse serving layer only understands listing-seen and probe facts, while PostgreSQL mutable truth needs catalog-complete state derivation and runtime control rows. Splitting the backfill by target keeps replay idempotent without inventing mismatched analytics tables. |
| 34 | During `dual-write-shadow`, runtime/operator read surfaces must still resolve from SQLite; only `polyglot-cutover` should switch `runtime-status` and other operator reads onto the platform path. | `vinted_radar/cli.py`, `vinted_radar/platform/health.py`, `tests/test_reconciliation.py` | S14 closeout verification caught a regression where `runtime-status` tried PostgreSQL in shadow mode just because platform writes were enabled. Honoring the published cutover read-path contract keeps shadow validation honest and prevents accidental dependence on live platform connectivity before read cutover. |
| 35 | Do not assume `fact_listing_change_events` is live just because ClickHouse V002 defines it; S13/S14 currently ingest only listing-seen and probe batches, so change-oriented marts need either a new projector/ingest path or an explicit derivation plan from existing rollups. | `infra/clickhouse/migrations/V002__serving_warehouse.sql`, `vinted_radar/platform/clickhouse_ingest.py`, `vinted_radar/services/projectors.py` | S15/T03 investigation found the warehouse schema advertises change/state-serving primitives, but the active cutover pipeline never populates them. Treating the table as live would produce empty or misleading price-change/state-transition marts. |
| 36 | When replaying discovery history into ClickHouse, carry page chunk metadata (`page_chunk_index`, `page_chunk_count`, `pagination_current_page`, `pagination_total_pages`, `next_page_url`) and emit follow-up-miss transitions only on the final chunk of a terminal catalog page. | `vinted_radar/services/full_backfill.py`, `vinted_radar/platform/clickhouse_ingest.py`, `tests/test_clickhouse_ingest.py`, `tests/test_full_backfill.py` | S15/T04 showed that replay chunking can otherwise fabricate missing-from-scan transitions before a page is fully reconstructed. Terminal-page metadata keeps derived change facts truthful during historical backfill. |
| 37 | Expose AI-ready analytical exports as stable ClickHouse mart views plus grouped evidence packs that carry manifest ids, source event ids, run ids, and concrete `evidence-inspect` examples. | `vinted_radar/query/feature_marts.py`, `infra/clickhouse/migrations/V002__serving_warehouse.sql`, `tests/test_feature_marts.py` | S15/T05 established that downstream AI and analytics consumers should not rescan raw fact tables ad hoc. The mart contract stays grounded only if every row and pack preserves explicit traceability back to observed windows and manifests. |
| 38 | Treat `scripts/verify_cutover_stack.py` as the authoritative post-cutover acceptance proof, and require platform-audit posture, fresh change facts, evidence-pack drill-down, and ClickHouse-vs-SQLite route parity in that proof. | `scripts/verify_cutover_stack.py`, `tests/test_cutover_smoke.py`, `README.md` | S15/T06 closed the migration only after the verifier proved not just platform health, but also warehouse freshness, evidence traceability, and the removal of the remaining SQLite analytical hot path. |
| 39 | Keep `/health`, `/runtime`, `/api/runtime`, and CLI `runtime-status` on an embedded bounded platform-audit snapshot; reserve full cross-store reconciliation for the explicit `platform-audit` command. | `vinted_radar/services/platform_audit.py`, `vinted_radar/dashboard.py`, `vinted_radar/cli.py` | The VPS shadow soak wedged the single-threaded dashboard once request paths started running full reconciliation over thousands of lake objects. Embedded audit keeps checkpoint/lifecycle truth visible without turning health endpoints into blocking parity scans. |
| 40 | On this workstation, if `lsp status` shows no active Python server, avoid workspace-wide `lsp diagnostics` during auto-mode task execution; use focused reads and pytest/shell verification instead. | auto-mode execution / local tooling | M003/S02/T01 stalled on an in-flight `lsp diagnostics` call after broad file reads. Until a working Python language server is confirmed in the active shell, whole-workspace diagnostics are a bad trade and can wedge task execution. |
| 41 | If a slice is already complete, any lingering `.gsd/runtime/units/execute-task-<mid>-<sid>-*.json` files for that slice should be treated as stale residue and parked before starting later units. | auto-mode runtime hygiene | M003/S02 resumed with old S01 task-unit JSON files still present in `.gsd/runtime/units/`, even though S01 was already complete. Those stale runtime units are local state, not authoritative progress, and can pollute later recovery/context decisions if left active. |
| 42 | Fail fast if a multi-lane runtime is pointed at a control-plane repository that does not accept `lane_name` on controller methods; do not silently collapse multiple lanes onto one shared controller row. | runtime control plane | M003/S02/T02 added threaded frontier/expansion orchestration, and the pre-existing PostgreSQL mutable-truth adapter still exposed only a singleton controller contract. A silent fallback would make `/runtime` truth ambiguous and couple unrelated lane pauses/errors together. |

## Lessons Learned

| # | What Happened | Root Cause | Fix | Scope |
|---|--------------|------------|-----|-------|
| 1 | Live discovery needed both broad seed coverage and low request volume for smoke verification. | Homme/Femme contain 405 leaf catalogs, so a full one-page sweep is real but heavier than a quick verification pass. | Keep the batch CLI default conservative and use `--max-leaf-categories` for smoke runs while still syncing the full seed registry every run. | S01+ |
| 2 | Live CLI output can fail on real titles even when the data pipeline is correct. | Public item titles can contain characters unsupported by the active terminal encoding. | Sanitize display output on the CLI boundary; keep JSON output lossless for machine inspection. | S02+ |
| 3 | Narrow fixture-only probe parsing left real 200 item pages unresolved. | The buy-state block on live item pages is embedded as escaped JSON text, not always in the same unescaped field order as the fixture. | Normalize escaped quotes before matching and keep unmatched pages as `unknown` instead of forcing a guess. | S03+ |
| 4 | Premium ranking becomes noisy fast when price context is too sparse. | Thin peer groups make percentile-based price boosts look precise even when they are effectively random. | Require explicit minimum support for each context tier and drop back to demand-led scoring when no trustworthy peer group exists. | S04+ |
| 5 | A first-pass live dashboard can look repetitive even when the UI is correct. | After a single discovery run, most listings are still `active` with one observation, so demand tables flatten and segment cards skew toward recent arrivals. | Treat that as an expected data-shape limitation, not a presentation bug; S06 needs repeated runtime history before the market read becomes richer. | S05+ |
| 6 | The live dashboard can truthfully show a `running` runtime cycle with zeroed latest-run scan counts while still displaying useful rankings and history. | `latest_run` reflects the in-progress discovery run, while rankings/freshness continue to derive from the last completed evidence already stored in SQLite. | Treat the runtime card and `/api/runtime` as the operator truth for in-flight state, and treat market tables as the latest completed evidence snapshot rather than assuming both surfaces update atomically. | S06+ |
| 7 | Healthy long-running runtime logs were being flagged as process errors by the tool harness. | The continuous CLI printed `0 failed scans` in normal success summaries, and the harness classified the word `failed` as an error signal even when the count was zero. | Phrase healthy discovery summaries positively (`all scans clean`) and reserve failure wording for non-zero failures so background-process health stays trustworthy. | S06+ |
| 8 | A limited `continuous` run can look like broad multi-day evidence when it is actually revisiting the same small subset of catalogs. | `_select_leaf_catalogs()` sorts leaf catalogs deterministically and `--max-leaf-categories` always takes the first N; there is no built-in rotation across cycles. | Use `--max-leaf-categories` only for smoke checks, and run closeout / credibility collection without that limit when the goal is broad market evidence. | S06+ |
| 9 | User feedback made clear that the M001 dashboard reads like an internal proof surface rather than a usable intelligence workspace. | M001 optimized for evidence/debug truth rather than broad-audience workflows. | Preserve the evidence backbone, but plan M002 around four distinct surfaces: market overview, full listing explorer, listing detail, and operator/runtime administration. | M002 planning |
| 10 | A server-side catalog or item fetch that works in one run can degrade into widespread 403 / Turnstile challenges a few hours later. | Access is mediated by challenge systems; availability is intermittent, not binary, and cannot be treated as a stable backbone just because one live run succeeded. | Instrument challenge rate explicitly and avoid assuming the current server-side scraping path is infinitely scalable or operationally reliable. | Acquisition strategy |
| 11 | The primary image URL timestamp is useful but not exact. | It often tracks upload time closely for fresh listings, but historical drift is large. | Model it as an estimated publication signal with explicit provenance/confidence alongside exact radar first-seen timestamps. | Listing model / dashboard |
| 12 | Older SQLite proof databases can become unreadable if schema bootstrap creates indexes for columns that are only added later by migration. | `connect_database()` executes the full schema before `_apply_migrations()`, so `CREATE INDEX ... created_at_ts` fails on pre-column DBs before `ALTER TABLE` runs. | Add new columns before dependent indexes, or move mutable indexes into versioned migrations so historical DBs remain openable. | DB evolution / closeout verification |
| 13 | Multi-day operator metadata is not enough for milestone sign-off if listing-history tables are unhealthy. | `data/vinted-radar.db` preserved several days of `runtime_cycles`, but malformed listing tables prevented truthful market-read verification. | Treat `db-health` and queryable history/state surfaces as part of closeout proof, not as optional cleanup after runtime has already accumulated. | Milestone closeout / operations |
| 14 | The planned S01 browser-demo database path is absent in a fresh local checkout. | The overview/browser proof is seeded through test fixtures, but `data/vinted-radar-s01.db` is not committed in the repo by default. | Recreate `data/vinted-radar-s01.db` locally from `tests.test_overview_repository._seed_overview_db(...)` before running the S01 dashboard CLI + browser verification flow. | M002/S01 verification |
| 15 | `/api/runtime` and `/health` can drift briefly if continuous-mode observer callbacks run before the next controller transition is persisted. | The runtime loop originally called the callback after a cycle finished but before writing the next `scheduled` / `paused` controller state, so one surface could momentarily read `idle` while another still showed the in-flight truth. | Persist the post-cycle controller transition before observer-facing callbacks, and treat this ordering as part of the runtime truth contract. | M002/S02 verification |
| 16 | A live dashboard process can look healthy in `systemctl` and `ss` while the public product is effectively dead if the backing SQLite file has ballooned into a corrupted or operationally unusable giant. | The VPS kept serving from a 61 GB `data/vinted-radar.db`; the process still listened on `:8765`, but real HTTP checks and `db-health` on that file stopped being trustworthy or responsive. | Archive the giant broken DB out of the live path, bootstrap a fresh healthy `data/vinted-radar.clean.db`, repoint services to that clean DB, and keep any realistic large-corpus acceptance on a separate healthy proof snapshot instead of trying to revive the giant file in place. | M002/S07 recovery |
| 17 | A healthy large SQLite corpus can still throw `OperationalError: database or disk is full` during runtime state refresh even when dashboard detail routes are fixed. | The runtime state-refresh path loaded `listing_state_inputs()` across the whole corpus twice per cycle just to choose a handful of probe targets and compute a global summary, pushing SQLite into expensive temp work on large datasets. | Select probe targets through a lightweight SQL query, and skip the full post-probe global state summary inside continuous runtime cycles; keep the expensive global summary for explicit CLI diagnostics instead. | M002/S08 runtime recovery |
| 18 | Proxy preflight can look falsely degraded if it probes Vinted directly without reproducing the collector's homepage warm-up first. | The first S09 preflight version hit Vinted reachability on raw proxy sessions, while the real collector warms the route on `https://www.vinted.fr/` before asking for `/catalog` or the API. That mismatch made healthy Webshare routes look like challenge hits. | Make preflight reuse the real transport posture: warm the route first, then probe the actual discovery API path. | M002/S09 |
| 19 | The first proxy-pool auto-cap that feels “safe” can still leave a lot of throughput on the table. | The initial S09 auto-cap of 12 looked reasonable from the first 12-leaf benchmark, but heavier live runs over 24 and 32 leaf categories showed concurrency 24 beating 12, 16, 20, 32, and 40 with zero scan failures on the 100-route Webshare pool. | Benchmark concurrency against a workload that exposes more parallelism than the current cap before freezing the default; for the current local Webshare pool, use an auto-cap of 24. | M002/S09 post-closeout tuning |
| 20 | Milestone code-diff verification can look falsely empty when closeout runs on the branch that is itself `main`. | `git diff --stat HEAD $(git merge-base HEAD main)` collapses to `HEAD..HEAD` when `HEAD` already points at `main`, even if the milestone contains substantial unmerged work relative to the remote integration base. | For milestone closeout on `main`, compare against `origin/main` (or the actual integration branch) rather than local `main` when proving non-`.gsd/` implementation work. | M002 closeout |
| 21 | A storage-migration milestone is not fully proved by route checks alone or by platform checks alone. | Product routes can stay green while cutover observability, change-fact freshness, or evidence traceability drift, and platform services can look healthy while public product parity regresses. | Close out polyglot cutovers with one acceptance bundle that checks route parity, platform audit posture, change-fact freshness, and evidence drill-down together; treat Docker-gated reruns as operational follow-up, not as a reason to ignore the combined proof contract. | M002 closeout |
| 22 | `platform-audit` can stay red after a healthy live shadow batch even when the mutable-truth write path is fixed. | The audit’s object-storage reconciliation only counts `capture_source=sqlite_backfill` manifests, and the `current_state` path expects the separate `postgres-current-state-projector` checkpoint rather than direct shadow writes alone. | For VPS shadow validation, treat a successful `batch`/`continuous` run plus growing PostgreSQL mutable-truth rows and clean logs as the primary proof; use backfill and downstream consumer runs only when the goal is a fully green audit posture. | M002 VPS shadow rollout |
| 23 | A healthy shadow collector can still make the public product look dead if runtime/health routes run full reconciliation inline or if shadow-mode runtime control-plane writes stop landing in SQLite. | The VPS dashboard used a single-threaded WSGI server, `/health` and `/api/runtime` called `platform-audit` synchronously, and shadow-mode CLI wiring still pushed runtime controller/cycle truth into PostgreSQL whenever writes were enabled. That combination blocked web requests and left runtime status `null` even while the scraper was healthy. | Keep embedded audit on request/runtime surfaces, and keep runtime control-plane writes on SQLite until `enable_polyglot_reads` is actually enabled. | M002 VPS shadow remediation |
| 24 | A lane-aware runtime rollout can fail on the VPS before the new runtime behavior is exercised at all. | The live host needed both the benchmark module imported by `vinted_radar.cli` and a migration-safe `lane_name` bootstrap path; copying only runtime/CLI files caused `ModuleNotFoundError` first and then `sqlite3.OperationalError: no such column: lane_name` when the legacy SQLite DB reopened. | When syncing M003/S02 runtime changes to the VPS, include `vinted_radar/services/acquisition_benchmark.py` and keep `idx_runtime_cycles_lane_started_at` creation behind migrations so legacy DBs add `lane_name` before the index is created. | M003/S02 VPS proof |
| 25 | Multi-lane runtime tests can fail with `sqlite3.OperationalError: database is locked` before any lane reaches discovery. | `connect_database()` re-ran WAL/schema/migration bootstrap on every new SQLite connection, so two lane threads opening the same fresh DB could contend during setup and kill the orchestrator before lane truth was persisted. | Guard file-backed SQLite bootstrap with a process-local per-path lock, set a busy timeout, and skip repeat schema/migration setup once the DB has already been initialized in-process. | M003/S02 closeout |
| 26 | Interrupted VPS benchmark runs can silently wedge later runtime proof windows by filling `/root/Vinted/data` with orphaned `*.benchmark-export-*.db` files until Docker PostgreSQL falls into `No space left on device` recovery loops. | Before the final S02 proof, repeated hung benchmark attempts left dozens of ~1.8–1.9 GB export snapshots on the VPS. The host disk hit 100%, `infra-postgres-1` became unhealthy, frontier cycles failed on PostgreSQL connection errors, and the dual-lane smoke could not complete honestly until the stale exports were deleted. | For multi-lane live benchmarks, avoid remote snapshot export by default, clean orphaned benchmark exports after interrupted runs, and treat sudden PostgreSQL recovery/rejecting-connections errors on the VPS as a likely disk-pressure symptom rather than a lane-runtime bug first. | M003/S02 VPS recovery |
